<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 设置为IUserDao接口方法提供sql语句配置 -->
<mapper namespace="com.chatRoot.dao.IUserDao">

    <select id="selectUser" resultType="User" parameterType="long">
        SELECT * FROM user WHERE id = #{id}
    </select>

    <select id="findbyname" resultType="Map" parameterType="String">
        SELECT  * FROM  USER  WHERE  username = #{username}
    </select>

    <select id="insertwu" resultType="Map" parameterType="Object">
        INSERT INTO user_lost_pick(item_name,user_name,contact,item_time,item_location,item_type,lostpost,tel) VALUES (#{item_name},#{user_name},#{contact},#{item_time},#{item_location},#{item_type},#{lostpost},#{tel})
    </select>

</mapper>